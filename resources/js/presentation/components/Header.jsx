export const Header = ({ h1 }) => (
    <header>
        <div
            style={{ marginTop: "0" }}
            className="w3-center w3-padding w3-teal"
        >
            <h1>{h1}</h1>
        </div>
    </header>
);
export default Header;
